PORT=8080
MONGO_URI=
JWT_SECRET_KEY=

# Cloudinary (for image & document uploads) — from cloudinary.com dashboard.
# Optional: if left empty, uploads are stored inline as a fallback.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
